
name: flutter_taxi_app_env_fixed
description: Flutter app with .env as asset
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_naver_map: ^1.0.2
  url_launcher: ^6.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/env
